#
"""
Dont include .python-version file
Use my custom .gitignore standard file
Setup git.
uv init doesn't actually setup uv venv, so what does it even mean?
Don't use the standard main.py file in root.
My pyproject standard replaces the uv init one - can mine work without first seeing the uv init generated pyproject.toml?
Readme.md should be generated to include the description (which may or may not exist yet but might be the only thing prompted for), and shouldninclude the helotree reference section.
"""
def run_uv_init_custom():
    pass
