[project]
name = "maxson-build-utils"
version = "0.1.0"
description = "Common tools for deployong packages to MSIX, DMG, DEB, and more. Templated approach to mkdocs."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
